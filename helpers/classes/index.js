'use strict';

/**
 * Expose classes
 */
const Mail = require('./mail');
const Response = require('./response');
const ResponseError = require('./response-error');

/**
 * Export
 */
module.exports = {
  Mail,
  Response,
  ResponseError
};
